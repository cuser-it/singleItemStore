# 单品商城 MVP 需求确认

版本：MVP V1
状态：已确认

## 1. 产品定位

系统是单品商城。网站首页同一时间只展示一个商品，不支持购物车、多商品同时销售和库存管理。

通过后台切换商品信息模板，实现不同商品之间的快速切换。例如保健品、电子产品可以分别保存为不同模板，切换后前台整体展示当前启用模板的内容。

这里的“商品信息模板”不是颜色、布局或视觉主题模板，而是一套完整的商品销售数据。

## 2. 商品信息模板

每个模板包含：

- 商品主图轮播
- 商品名称
- 促销信息
- 商品参数
- 商品详情图
- 首屏精选用户评价及评价首图
- 商品套餐和套餐价格
- 客服二维码及客服链接配置

后台可以创建、编辑、预览、启用和停用商品信息模板。网站首页只读取当前启用的一个模板。

商品模板切换后，以上商品展示内容整体切换。

### 2.1 商品套餐

每个模板支持创建多个商品套餐，例如 A 套餐、B 套餐、C 套餐。每个套餐独立设置：

- 套餐名称
- 套餐价格
- 启用状态
- 展示顺序

MVP 不设置库存，不进行库存扣减，也不因为下单改变库存。

### 2.2 商品评价

评价只维护首页首屏展示的精选评价，不开发完整评价系统。每条精选评价可配置评价文字、评价首图和评价标签。

## 3. 图片和文件管理

图片存储使用已部署的 RustFS。后端负责图片上传、查询、排序、绑定和删除，前台只使用后端返回的图片地址。

图片只能由管理员后台上传和删除。

### 3.1 商品主图

- 后台上传多少张，前台轮播展示多少张
- 支持调整展示顺序
- 图片按顺序循环或切换展示

### 3.2 商品详情图

- 不使用富文本编辑器
- 只支持上传图片
- 支持调整图片顺序
- 前台按顺序垂直拼接展示，形成长图效果
- 支持图片懒加载

### 3.3 图片限制

后端需要同时校验文件格式、文件大小和图片实际内容，不能只根据文件扩展名判断。

MVP 默认支持 JPG、PNG、WebP，并分别配置主图和详情图的大小限制。详情长图可能较大，限制应结合图片像素尺寸、总数量和前台加载性能确定。后续可以增加缩略图、WebP 转换和 CDN 加速。

## 4. 网站配置

网站配置跟随商品信息模板一起配置，包括：

- 网站 Title
- 网站名称
- favicon
- 客服二维码
- 客服链接展示或跳转方式
- 货到付款开关（预留，MVP 默认关闭）

## 5. 客服二维码管理

管理员可以在模板中添加多个客服二维码或客服链接。

上传企业微信二维码后，后端解析二维码中的 URL 并保存。管理员也可以手动添加以 `https://` 开头的客服 URL。

后台支持：

- 添加多个客服二维码或链接
- 查看解析出的 URL
- 手动修正或添加 HTTPS URL
- 启用、停用和删除链接
- 设置当前模板使用的客服链接
- 选择前台渲染二维码或跳转 URL

二维码解析失败时允许管理员手动输入 HTTPS URL。后端必须校验 URL 协议，禁止危险协议。

支付成功后，前台展示客服微信引导页面，并根据当前商品信息模板的客服配置进行渲染或跳转。

## 6. 下单与支付

MVP 第一版接入易支付 V2，不接入微信支付和支付宝官方接口。后续通过支付适配器扩展其他支付渠道。

下单流程：

```text
用户选择套餐并填写收货信息
    -> 后端读取当前启用模板和套餐
    -> 后端以数据库套餐价格计算订单金额
    -> 创建待支付订单和订单快照
    -> 调用易支付 V2
    -> 用户完成支付
    -> 后端接收并校验异步通知
    -> 订单标记为已支付
    -> 前台展示客服微信引导页
```

前端提交的价格只能用于展示，后端禁止信任前端价格参数。订单金额必须以后端数据库中的套餐价格为准。

支付回调必须校验订单号、签名和实际支付金额，并支持重复回调幂等处理。后续预留支付状态主动查询能力。

MVP 暂不开放货到付款，但后台保留货到付款开关配置。

## 7. 订单和发货

订单至少保存商品模板、套餐、商品名称和价格快照，以及收货人信息和支付信息。商品或套餐后续修改后，历史订单数据不变。

下单信息包括：

- 收货人姓名
- 收货人手机号
- 收货地址
- 选择的商品套餐

支持用户通过收货人手机号查询订单。查询接口需要校验手机号格式、限流，并对收货地址等敏感信息脱敏。

后台订单功能包括：

- 查看订单列表
- 查看订单详情
- 查看支付状态
- 填写物流公司
- 填写物流订单号
- 修改发货状态

发货状态只有两个：

- `WAIT_SHIP`：待发货
- `SHIPPED`：已发货

系统不设置“已完成”状态，也不接入物流轨迹判断。因为无法可靠判断包裹是否完成配送，订单一旦标记为“已发货”，业务上即视为已完成，不再增加第三种状态。

MVP 暂不接入快递物流状态公共接口。后续如果需要展示运输轨迹，再由后端接入快递查询服务，并且不在前端暴露第三方密钥。

## 8. 管理员和操作日志

MVP 使用单管理员账号。

管理员密码必须使用安全哈希保存，不能明文存储。

### 8.1 登录日志

记录：

- 登录时间
- 是否登录成功
- IP 地址
- User-Agent
- 访问来源 Referer

### 8.2 管理操作日志

记录：

- 操作管理员
- 操作时间
- 操作类型
- 操作资源和资源 ID
- 操作内容摘要
- 请求 ID
- IP 地址
- User-Agent

需要记录的操作包括修改配置、开关状态变化、商品模板切换、图片上传、图片删除、客服链接变更、套餐价格修改、订单发货信息修改和管理接口访问。

日志中不得记录管理员密码、支付密钥或其他不必要的敏感信息；请求体需要脱敏。

## 9. 技术栈

前端：

- React
- TypeScript
- Vite
- React Router
- TanStack Query
- Ant Design
- React Hook Form
- Zod

后端：

- NestJS
- TypeScript
- Prisma
- PostgreSQL

文件存储：

- RustFS，使用 S3 兼容接口

MVP 阶段暂不引入 Redis。后续按限流、缓存、支付查询和图片处理需求增加。

## 10. MVP 实施顺序

1. 初始化 React 前端和 NestJS 后端
2. 设计数据库模型和迁移
3. 实现单管理员登录
4. 实现商品信息模板管理
5. 实现商品套餐管理
6. 实现 RustFS 图片上传、排序和删除
7. 实现模板预览和当前模板切换
8. 将 `dome/cankao/index.html` 拆分为 React 页面
9. 前台从后端加载当前启用模板
10. 实现订单创建和手机号订单查询
11. 实现后台订单、物流公司和物流单号管理
12. 实现客服二维码解析和客服链接切换
13. 接入易支付 V2
14. 增加接口测试、数据库测试和浏览器验收测试
